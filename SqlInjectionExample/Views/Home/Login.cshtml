@model SqlInjectionExample.Models.Credentials
@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>

@using (Html.BeginForm(null, null, FormMethod.Post))
{
    @Html.EditorForModel();

    <input type="submit" value="Login" />
}

